==============
muses.thesauri
==============
What it does.

Usage examples
==============

.. code-block:: python

    client = ThesauriClient(user='user', password='secret')
    key = client.lookup_key('en', "scarabee")
d